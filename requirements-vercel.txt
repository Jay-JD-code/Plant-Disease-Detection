# Minimal requirements for Vercel deployment (without PyTorch)
Flask==2.3.3
Werkzeug==2.3.7
Pillow>=10.0.0
numpy>=1.21.0

# NO PyTorch - too large for Vercel
# App will automatically run in demo mode without PyTorch
